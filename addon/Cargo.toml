[package]
name = "bare-arti-addon"
version = "0.0.1"
edition = "2021"
license = "MIT"
publish = false

[lib]
crate-type = ["staticlib"]
path = "src/lib.rs"

[dependencies]
bare-arti = { path = ".." }
tokio = { version = "1", features = ["rt-multi-thread"] }
